#include <bits/stdc++.h>

using namespace std;

int main()
{
    int n ; 
    cin>> n ; 
    while(n <= 9000){
         ++n ;
        int temp = n ;
        int a = temp % 10 ; 
        temp = temp/10 ; 
        int b = temp % 10 ; 
        temp = temp/10 ; 
        int c = temp % 10 ; 
        temp = temp /10 ;
        int d = temp % 10 ; 
        temp = temp / 10 ; 
        
        if(  a != b && a != c && a != d && b != c && b!= d && c!= d){
            cout<<n<<endl ; 
            break ; 
        }
    }
    return 0;
}
