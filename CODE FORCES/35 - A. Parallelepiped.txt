#include <bits/stdc++.h>

using namespace std;

int main()
{
    int a , b , c ; 
    cin>>a>>b>>c ; 
    
    int s1 = sqrt((a*b)/c) ;
    int s2 = sqrt((a*c)/b) ;
    int s3 = sqrt((b*c)/a) ;
    
    int ans = (s1+s2+s3) * 4 ; 
    
    cout<<ans ; 

    return 0;
}