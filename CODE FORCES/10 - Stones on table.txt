#include <bits/stdc++.h>

using namespace std;

int main()
{
    int n ; 
    cin>>n ; 
    string str ; 
    cin>>str ; 
    int count = 0 ;
    int i=0 ; 
    while(i < str.size()-1){
        if(str[i] == str[i+1]){
            count++ ; 
        }
        i++ ; 
    }
    cout<<count;

    return 0;
}